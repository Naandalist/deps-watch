{
  "name": "deps-watch",
  "version": "1.0.0",
  "private": true,
  "description": "A daily board of JavaScript dependency security issues that contributors can help fix.",
  "type": "module",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "build:ghpages": "npx @11ty/eleventy --pathprefix=/deps-watch/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "js-yaml": "^4.1.0"
  }
}
